# OS files
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/
*.py[cod]
*.class
*.pyc
.pytest_cache/
.coverage

# Node / Web
node_modules/
.next/
dist/
build/
.cache/
*.tsbuildinfo

# Explicitly track DATABASE folder and SQL assets
!DATABASE/
!DATABASE/**

# Agent internals & logs
.agents/

# Local Developer / Active User Identity (Machine-specific, gitignored)
.active_user

# Environment variables & secrets
.env
.env.local
.env.*.local
*.pem
*.key

# Durable local store (file-backed prototype DB). Holds user records with
# password hashes + salts, exactly like a real users table - NEVER commit.
.data/
store.json
