# CryptoTrace ML sidecar — Python dependencies.
# Pinned to majors that are known-good on Python 3.11–3.13.
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.6,<3.0
numpy>=1.26
networkx>=3.2
xgboost>=2.0
scikit-learn>=1.4        # training metrics + train/test split only
# Optional — only imported when NEO4J_URI is set. Safe to omit for the
# NetworkX-only default path.
neo4j>=5.19 ; python_version >= "3.9"
